'hello'

"hello"

'''hello
world
'''

"""hello
world
"""

----

# Сложение строк
a='hello'
b='world'
c=' '

a + c + b
a + ' ' + b

a*5

a + str(5)

---------

len(a)	#длина строки

----------

# проверка есть ли символ или строка в строке
'h' in s

-----------

'hello' == a   True

'a' > 'r'	False  Сравнение кода ASCII символа
ord('r')	Значение кода ASCII символов

-------------

s = 'hello world'
pritnt(s[0])  # Результат h

pritnt(s[-1])  # Результат d  -1 это послед индекс -2 это предпоследний и т.д.

----------
Срез - взять часть из строки

s[2:6]	# Берем с индекса 2 по 5 номер
s[4: ]	# С индекс и до конца
s[ :5]	# С начала и по 4
s[::2]	# От начала до конца через один символ
s[::-1]	# Строка наоборот

------------

Замена в строке

s = s[:4] + 'a' + s[5:]

--------------
МЕТОДЫ

'hello'.upper()     # Перевод символов верхний регистр
'HELLO'.lower()     # Перевод символов нижний регистр
'HELLO MAN'.lower() #Возвращает копию строки, в которой каждое новое слово начинается с заглавной 		     буквы и продолжается строчными.


s.count('o')	#Сколько раз 'o' встречается в строке
s.(count('o' , 0, 6)) # Указываем что ищем с 0 по 5 позицию

----

s.find('0', 0, 6)	# Поиск позиции символа или подстроки в строке, возвращает первую позицию
s.rfind()		# Поиск с конца
Если нет символа возвращает -1

s.index('z')		# Поиск символа/подстроки и возвращает индекс
Если нет символа возвращает ошибку

------

s.replace('o', '!!!')	# Замена каждого символа 'o'
s.replace('o', '')	# Удалить все символы 'o'
s.replace('o', '', 2)	# Удалить только 2 первых символа 'o'

----

s.isalpha()	Thrue	# Проверяет состоит ли строка только из букв пробелы датут False
s.isdigit()	Thrue	# Проверяет состои ли из цифр для Float False

------

d = '111'
d.rjust(5)	# Если строка меньше 5 символов добавит пробелы или указаный
            # символ и прижмет нашу cтроку вправо
'  111'
d.rjust(5, '0')
'00111'

d.ljust		# Прижмет строку влево

--------
# split разбивает строку на лист если разделитель пробел (можно другой
# разделитель указать) разделитель удаляется, так же можно указать количество разделений
name = 'ivano ivan ivanovich'
name.split()
['ivanov', 'ivan', 'ivanovich']

# Кол-во слов в name
len(name.split())

--------
# join соединяет строковые элементы

s =['55', '888', '98']
'='.join(s))
55=888=98

--------
# strip удаляет в строке знаки пробелов и пероноса на новую строк
# rstrip удаляет только справа
# lstrip удаляет только слева

s.strip

-------
можно делать цепочки из методов

s.upper().replase(...).lower.  и т.д.


